public class BST_Closet_Binary_Search_Tree_Value {
}

/**
 * find lowerbound, Node with largest value which is smaller than target
 * find upperbound, Node with smallest value which is larger than target
 *
 * compare these two values
 * lowbound node null, return upperboud node
 * upperbond node null, return lowerbound
 * lowerbound closer to target return lowerbound
 * upperbound closer to target return upperbound
 *
 */
